---
title: get_compatibility
description: Two dates → both identities plus a scored compatibility breakdown.
tag: Production
---

<Badge color="green">Production</Badge>

<Info>
  **Status:** Production.
</Info>

Two dates in → both Cosmic-card identities, connections between them, and a scored breakdown. Scoring follows the cardology connection system (Cards of Destiny / Robert Camp tradition).

<Info>
  **When to use:** comparing **two** people only. For a single date’s moon, season, Rx, calendars, weekday, leap year, or day_ruler → [`get_date`](/tools/get-date).
</Info>

<Info>
  **One call returns** two full date profiles plus a weighted compatibility score and cross-wheel aspects — ~1,500 deterministic fields from two dates. Each side’s `cosmic.planetary_spread` includes **`spread_pos`** (1–14) — sort by that field; see [get_date → Planetary spread order](/tools/get-date#planetary-spread).
</Info>

| | |
|---|---|
| **Input** | `date_a`, `date_b` (YYYY-MM-DD) |
| **Range** | Same coverage bands as [`get_date`](/tools/get-date) — `temporal_context.date_a/b.band` = `full` inside 1900–2100; `extended` nearby; farther out → `out_of_range` with `payload_scope=birth_card_only` |
| **MCP URL** | `https://mcp.geometry.app/mcp` |

**Ask:**

> How compatible are people born 1985-03-15 and 1990-11-08?

Live payloads are large (~85 KB) because each side embeds a full date profile. Below is a teaching excerpt — wire keys `birth_card_a` / `birth_card_b` are abbreviated; each matches the [`get_date`](/tools/get-date) shape (Cosmic card + systems).

**Full payload / raw JSON:** call this tool — live response is the complete object (no verbosity flag). Chat may summarize; ask for the raw tool result if the UI truncates.

## Example response

```json
{
  "flags": {
    "is_same_card": false,
    "is_self_pair": false,
    "is_wild_card": false
  },
  "summary": "Compatibility for two dates in the published 1900–2100 window.",
  "life_path_a": 5,
  "life_path_b": 2,
  "birth_card_a": "… full date profile: Cosmic card, tarot, medicine, Lenormand, zodiacs, moon phase, season, retrogrades, runes, ogham, geomancy, I Ching, and more …",
  "birth_card_b": "… full date profile (same shape) …",
  "temporal_context": {
    "date_a": { "band": "full", "payload_scope": "full" },
    "date_b": { "band": "full", "payload_scope": "full" }
  },
  "true_sidereal_a": "…",
  "true_sidereal_b": "…",
  "card_connections": [
    {
      "type": "jupiter",
      "direction": "a_to_b",
      "planet_to_planet": "harmonious"
    }
  ],
  "element_to_element": "neutral",
  "compatibility_score": {
    "bonuses": {
      "element_bonus": 0,
      "scoring_policy": "v1",
      "same_card_match": 0,
      "self_pair_penalty": 0,
      "ruling_planet_bonus": 0,
      "true_sidereal_bonus": -1,
      "chinese_zodiac_bonus": -2
    },
    "card_score": {
      "a_to_b": 3,
      "b_to_a": 0,
      "combined": 3,
      "connection_count_total": 1
    },
    "overall_score": 1,
    "interpretation": "neutral",
    "ruling_planet_to_planet": {
      "a_to_b": "neutral",
      "b_to_a": "neutral",
      "bonus_score": 0,
      "card_a_planet": "mars",
      "card_b_planet": "venus"
    }
  },
  "true_sidereal_aspect": "quincunx",
  "chinese_zodiac_aspect": "quincunx"
}
```

`chinese_zodiac_aspect` can also be `harmonic_pair` (Chinese secret-friend pair — not a Western aspect). See [What you get → Aspect map](/payload-shapes#aspect-map).

## Related

- [get_date](/tools/get-date) — one date identity  
- [get_name](/tools/get-name) — name → Cosmic cards
